#!/bin/bash

MOZIO_PATH=/home/humitos/mozio
MOZIO_REPOSITORIES="data-entry mozio mozio_auth mozio-commons mozio-locations mozio-search ondemand notifications errors"

for REPO in ${MOZIO_REPOSITORIES}; do
  echo "Updating '${REPO}'...";
  git --git-dir ${MOZIO_PATH}/${REPO}/.git fetch origin;
done
